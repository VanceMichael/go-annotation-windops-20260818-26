import {render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {describe,expect,it} from 'vitest';import {EntityTable} from './EntityTable';
describe('EntityTable',()=>{it('renders status and paginates stable rows',async()=>{const rows=Array.from({length:12},(_,index)=>({id:'work-'+String(index+1).padStart(2,'0'),status:index===0?'failed':'assigned',summary:'Turbine task '+(index+1),updated_at:'2026-08-18T09:00:00Z'}));render(<EntityTable rows={rows} details={['summary']}/>);expect(screen.getByText('work-01')).toBeInTheDocument();expect(screen.getByText('failed')).toBeInTheDocument();await userEvent.click(screen.getByTitle('Next Page'));expect(screen.getByText('work-12')).toBeInTheDocument();expect(screen.queryByText('work-01')).not.toBeInTheDocument()})});

