import {useQuery} from '@tanstack/react-query';import {Card,Col,Progress,Row,Statistic,Table,Typography} from 'antd';import {api} from '../api/client';import {ErrorState,Loading} from '../components/State';import {WorkspaceHeader} from '../components/WorkspaceHeader';
const labels:Record<string,string>={farms:'场群',turbines:'机组',weather_windows:'气象窗口',campaigns:'维护批次',crew_members:'海上人员',vessels:'作业船',permits:'作业许可',work_orders:'作业单',dispatches:'航次',evidence:'证据',outbox_jobs:'待发布事件',audit_events:'审计事件'};
export default function Overview(){const query=useQuery({queryKey:['overview'],queryFn:({signal})=>api.overview(signal),refetchInterval:30000});if(query.isLoading)return <Loading/>;if(query.isError)return <ErrorState error={query.error} retry={()=>void query.refetch()}/>;const data=query.data??{};const readiness=Math.min(100,Math.round(((data.permits??0)+(data.work_orders??0)+(data.evidence??0))/Math.max(1,(data.campaigns??1)*3)*100));const rows=Object.entries(data).map(([key,value])=>({key,label:labels[key]??key,value}));return <section><WorkspaceHeader title="海上作业态势" subtitle="东部浅滩场群 · 计划、海事与现场执行统一视图" onRefresh={()=>void query.refetch()} busy={query.isFetching}/><Row gutter={[16,16]}><Col xs={24} md={8}><Card><Statistic title="在册机组" value={data.turbines??0} suffix="台"/></Card></Col><Col xs={24} md={8}><Card><Statistic title="维护批次" value={data.campaigns??0} suffix="个"/></Card></Col><Col xs={24} md={8}><Card><Typography.Text type="secondary">资料完备度</Typography.Text><Progress percent={readiness} strokeColor="#167a5b"/></Card></Col></Row><Typography.Title level={3}>运行对象</Typography.Title><Table size="middle" pagination={false} dataSource={rows} columns={[{title:'对象',dataIndex:'label'},{title:'数量',dataIndex:'value',align:'right'}]}/></section>}

